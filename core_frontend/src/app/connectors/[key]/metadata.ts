import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Connector Plugin | Connector Framework Manager",
  description: "Connector-specific features and sample plugin UI",
};
